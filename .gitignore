node_modules
dist
*.log

.DS_Store

spacetime.local.json

# Local Claude Code settings (not code)
.claude/
tsconfig.tsbuildinfo

# Vercel
.vercel

# Local secrets — never commit (use for OPENROUTER_API_KEY in vercel dev)
.env
.vercel
